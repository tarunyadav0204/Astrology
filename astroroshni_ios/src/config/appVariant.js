/**
 * iOS App Store educational variant (AstroRoshni Study).
 * When true: Learn-only chat (backend mode=lab), hide prediction routes/tabs, classic white/grey theme.
 */
export const IS_ASTROLOGY_ONLY = true;
