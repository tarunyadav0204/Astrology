/**
 * iOS App Store variant: astrology tools only, no prediction/chat features.
 * When true: hide Ask/Events tabs, prediction menu items, FAB; use classic (white/black) theme.
 */
export const IS_ASTROLOGY_ONLY = true;
